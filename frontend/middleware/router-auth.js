/*import { auth } from "~/plugins/firebase.js";

export default function({ redirect, route }) {
  auth.onAuthStateChanged(user => {
    if (user && route.name === "login") redirect("/account");
    else console.log("Not on login page / No user");
  });
}
*/
